# Alterworld Hub


## Setup

Make sure to install the dependencies:

```bash
yarn install
```

## Running

Start the server on `http://localhost:3000`:

```bash
# dev
yarn run dev

# build
yarn run build

# preview
yarn run preview
```